# Publication copies

The original outputs remain unchanged in the separate local `paired-testing-2026-09-10-metKBx` test directory. These repository copies retain the original wording, findings, severities, coverage, statuses and timestamps, including errors.

Only these substitutions were made:

- Each completed run's timing file replaces one local Python executable path with `<python3.12-executable>`.
- The Tamil browser timing file replaces one local validator path with `<marketplace-root>/tests/validate_report.py`.
- The Arngren browser report replaces one public bank-account value and one public payment-phone value with descriptive redaction tokens. These values were unnecessary to establish the auditor's claimed workflow observation.

The interrupted Arngren text file is renamed `report-at-interruption.json` without repairing its invalid contents. The other report copies have no substitutions. Raw crawl bundles are not included: they may contain unnecessary credential-like URLs or payment details. The reviewed PDF is linked at its public university URL rather than redistributed in this repository.

Parent validation and timing observations are in `parent-checks.json`. The independent reviewer notes and top-level report are evaluator-authored records, not output authored by the tested agents. Review conclusions must not be silently substituted into an original audit or counted as an independent corrected pass.

The no-browser timing record uses `available: false` for several tools, but its reason says instruction-restricted. Treat that as prohibited usage, not evidence that those capabilities were physically removed from the host.
