# Correction note

This corrected copy was manually reviewed against the updated `tool-availability.md` manual-check guidance and `report-schema.json` after supplied parent feedback. The only schema correction is `coverage.modes`: host tool names `web_fetch` and `web_search` were replaced with schema enums `static`, `retrieval`, and `off_site`.

The original artifact was not altered. This correction did not browse again, execute Python/scripts/validator, use a browser, or independently verify runtime behavior. It preserves the initial session's explicit no-runtime restrictions and records manual review rather than automated validation. Session reuse from the prior review is disclosed.
